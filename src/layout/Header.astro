---
import { css } from "../../styled-system/css";
import { gradientBox } from "../../styled-system/patterns";
import Button from "../components/Button.astro";
import Link from "../components/Link.astro";

interface Props {
  siteTitle: string;
}

const { siteTitle } = Astro.props;
---

<header
  class={gradientBox({
    position: "sticky",
    top: 0,
    height: "headerHeight",
    px: 4,
    display: "flex",
    alignItems: "center",
    justifyContent: ["center", "start"],
    zIndex: "header",
  })}
>
  <slot>
    <Link
      href="/"
      className={css({
        color: "white",
        _hover: {
          color: "primary.200",
          textDecoration: "none",
        },
      })}
    >
      <h1 class={css({ fontSize: "xl" })}>{siteTitle}</h1>
    </Link>
    <div class={css({ flexGrow: 1, display: ["none", "none", "block"] })}></div>
    <div class={css({ display: ["none", "none", "block"] })}>
      <Button className="toggle-color-mode" size="md" colorPalette="primary">
        color
      </Button>
    </div>
  </slot>
</header>
