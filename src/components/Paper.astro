---

---

<div class="paper">
  <slot />
</div>
<style>
  .paper {
    background-color: rgba(var(--primary), var(--alpha));
    border-radius: var(--border-radius);
    padding: 16px;
    line-height: 1.6;
  }
</style>
